# Les captures de diagnostic contiennent le DOM complet et les reponses reseau.
# Les valeurs sensibles sont masquees (cf mpp/site.py, redact) mais ces fichiers
# n'ont rien a faire dans l'historique git.
state/captures/
*.tmp
__pycache__/
*.pyc
run.log
